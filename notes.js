function addNotes(title, message) {}

function getNotes() {}

function removeNote(title) {}

function getNote(title) {}

function loadNotes() {}

module.exports = {
  addNotes,
  getNote,
  getNotes,
  removeNote,
  loadNotes,
};
